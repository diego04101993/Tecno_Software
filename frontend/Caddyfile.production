{
    servers {
        trusted_proxies static private_ranges
    }
}

{$APP_DOMAIN} {
    root * /srv
    encode zstd gzip
    try_files {path} /index.html
    file_server
}

{$API_DOMAIN} {
    reverse_proxy backend:8000
}
